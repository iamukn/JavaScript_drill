function greet() {
//console.log('Hello world!');
  return 1;
}

greet();


let sum = x =>  {x + 2;};
sum(2);


const add = (x, y) => {
  x + y;
};
add();

name = undefined;

newName = name ?? "Ukn";
